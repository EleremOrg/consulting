{
  "name": "elerem-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "Elerem — Independent software consultancy website built with Astro.",
  "scripts": {
    "favicons": "node scripts/build-favicon-round.mjs",
    "predev": "pnpm run favicons",
    "prebuild": "pnpm run favicons",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev",
    "check": "astro check --minimumSeverity warning",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.8",
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^6.1.9",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "@tailwindcss/vite": "^4.2.4",
    "tailwindcss": "^4.2.4",
    "wrangler": "^4.85.0"
  }
}

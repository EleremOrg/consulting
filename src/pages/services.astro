---
/**
 * /services has been merged into the home page.
 * Keep this route alive as a permanent redirect so any inbound links
 * (search results, social shares, business cards) still land on the
 * services section of the home page.
 */
return Astro.redirect('/#services', 301);
---
